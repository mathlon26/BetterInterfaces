package be.mathijsfollon.betterInterfaces.api.events;

/**
 * Interface for classes that can listen to menu events.
 * Implementations should use the {@link MenuEventHandler} annotation on methods
 * to mark them as event handlers.
 */
public interface MenuEventListener {
}
